pipeline
{
    agent any
    stages
    {
        stage("build gradle project")
        {
            steps{withGradle(jdk: 'JAVA-HOME', gradle: 'GRADLE_HOME') {
            
       sh'gradle build'
}}
        }
    }
}
